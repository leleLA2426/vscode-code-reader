import * as vscode from 'vscode';

export function generateReaderHtml(
  content: string,
  language: string,
  filePath: string,
  baseUri: vscode.Uri,
): string {
  // Placeholder - actual HTML generation is handled inline in readerPanel.ts
  return '';
}
